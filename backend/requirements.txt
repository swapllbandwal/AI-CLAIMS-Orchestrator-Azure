fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0,<2.0

# LLM — Anthropic SDK pointed at Microsoft Foundry
anthropic>=0.39,<1.0

# Azure Computer Vision
azure-ai-vision-imageanalysis>=1.0,<2.0
azure-core>=1.30,<2.0

# HTTP client (for v3.2 Brands REST call)
httpx>=0.27,<1.0

# RAG — local embeddings, no external service.
# numpy>=2.1 needed for Python 3.13 wheels.
sentence-transformers>=3.0
numpy>=2.1,<3.0

# Image handling
Pillow>=10.4,<12.0
